# Package bytes and pinned Lua templates must survive Windows checkouts unchanged.
* text=auto eol=lf
# Preserve upstream-derived templates with their original mixed line endings.
tools/raw_overrides/** -text
*.zip binary
